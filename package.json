{
  "name": "babel-test",
  "version": "0.0.1",
  "description": "test",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf ./dist && cross-env NODE_ENV=production babel --verbose ./src -d ./dist"
  },
  "author": "eeve",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.0.0-beta.47",
    "@babel/core": "^7.0.0-beta.47",
    "@babel/preset-env": "^7.0.0-beta.47",
    "babel-preset-minify": "^0.4.3",
    "cross-env": "^5.1.4",
    "rimraf": "^2.6.2"
  }
}
